/*


class based components (have state by default)
    have access to state
    have access to lifecycle methods
    access props and state via this keyword


funcitonal components (functions that take in props and return JSX)
    have access to state
    do not have access to lifecycle methods but they DO have access to lifecycle hooks
    access state and props as direct objects (props... or state...)

*/
