/*
    2 way binding means passing data from parent to child and from child to parent

    e.g. in child component, set the value = {props.name} (name from the parent)

    fo 2 way binding I need to handle the changes with an onChange event
*/
