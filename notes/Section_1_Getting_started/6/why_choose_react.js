/*
    UI state becomes difficult to manage with vanilla JS

        manually target elements in the DOM (if we change the structure of HTML, we 
        need to change the selectors, etc..)

        it allows us to focus on business logic

        big community/ecosystem
*/
