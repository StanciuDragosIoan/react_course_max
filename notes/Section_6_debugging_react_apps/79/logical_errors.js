/*
    cannot read X... property of undefined (this means the X property does not
        exist not the event for instance)

    logical errors = no error msg but something does not work

        *find them with chrome debug tools ran through the code
*/
